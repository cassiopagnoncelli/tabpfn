% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init.R
\name{tabpfn_init}
\alias{tabpfn_init}
\title{Initialize the TabPFN runtime (Python env, HF token, local libs)}
\usage{
tabpfn_init(
  search_paths = NULL,
  prefer_venv = TRUE,
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{search_paths}{Character vector of directories to scan for \code{.venv} and
\code{builds/library}. Defaults to the working directory and its parent.}

\item{prefer_venv}{Logical; if \code{TRUE}, prefer a \code{.venv/bin/python} when choosing Python for reticulate.}

\item{force}{Logical; if \code{TRUE}, override an existing \code{RETICULATE_PYTHON} when a matching
\code{.venv/bin/python} is found.}

\item{quiet}{Suppress messages when \code{TRUE}.}
}
\value{
Invisibly returns \code{TRUE} on completion.
}
\description{
This helper is called automatically on package load, and can also be invoked
manually when working in development environments. It attempts to:
\itemize{
\item prepend a project-local \code{builds/library} to \code{.libPaths()} if present;
\item set \code{RETICULATE_PYTHON} to a nearby \code{.venv/bin/python} when one exists
and no Python has been chosen yet;
\item ensure Hugging Face tokens are surfaced from a \code{.hf_token} file.
}
}
